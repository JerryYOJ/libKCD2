#pragma once
#include <cstdint>

// -----------------------------------------------
// wh::guimodule::E_ButtonId -- KCD2 WHGame.dll 1.5.6 (kd7u).  Enum wrapper, Type : uint8.
// -----------------------------------------------
// Nested-Type enum wrapper (rttr property mangling `W4Type@E_ButtonId@guimodule@wh@@`).
// Identity key of C_UIMenuButton (byte @element+0x08; C_MenuButtonDatabase's
// DefaultKeyExtractor keys on it). ENUMERATOR NAMES/VALUES NOT RECOVERED (rttr
// enumeration registration not traced) -- do not invent values; extend when certified.

namespace wh::guimodule {

struct E_ButtonId {
    enum Type : uint8_t {};   // enumerators UNVERIFIED
};

}  // namespace wh::guimodule
