-- generated by KCD2/analysis/luautils/tools/generate_skald_lua.py; do not edit by hand
-- SKALD augmentation for wh::combatmodule::C_CombatTutorialNode
local raw_skald = SKALD
if type(raw_skald) ~= "table" then
    error("[LuaUtils] SKALD node wh::combatmodule::C_CombatTutorialNode: native SKALD table missing")
end
local Class = wh.combatmodule.CombatTutorialNode
if type(Class) ~= "table" or Class.__cpp_name ~= "wh::combatmodule::C_CombatTutorialNode" then
    error("[LuaUtils] SKALD class identity mismatch at wh.combatmodule.CombatTutorialNode")
end
local generated = rawget(Class, "__skald_generated_node")
if generated ~= nil then
    if generated ~= "wh::combatmodule::C_CombatTutorialNode" then
        error("[LuaUtils] incompatible generated SKALD node at wh.combatmodule.CombatTutorialNode")
    end
    return Class
end
local __applyUpdates = Skald.__applyUpdates
if type(__applyUpdates) ~= "function" then
    error("[LuaUtils] SKALD update dispatcher missing")
end
local __registerNode = Skald.__registerNode
if type(__registerNode) ~= "function" then
    error("[LuaUtils] SKALD node registrar missing")
end
local __invalidateNode = Skald.__invalidateNode
if type(__invalidateNode) ~= "function" then
    error("[LuaUtils] SKALD node invalidator missing")
end
local function readonly(values, label)
    return setmetatable({}, {
        __index = values,
        __newindex = function()
            error(label .. " is read-only", 2)
        end,
        __metatable = false,
    })
end

---@class wh.combatmodule.CombatTutorialNode.CreateArgs
---@field IsActive boolean? Native IsActive data input.
---@field Tutor userdata? Native Tutor object input (RTTR handle or wrapper).
---@field Domain userdata? Native Domain object input (RTTR handle or wrapper).
---@field InitialPhase integer? Native InitialPhase data input.

---@alias wh.combatmodule.CombatTutorialNode.OutputName
---| "OnTutorialEnd"
---| "OnPhaseStart"
---| "Phase"
---| "OnSuccessEvent"
---| "OnFailEvent"
---| "OnPhaseSkipped"
---| "Tracker"

---@class wh.combatmodule.CombatTutorialNode.Outputs
---@field OnTutorialEnd "OnTutorialEnd" Native trigger output.
---@field OnPhaseStart "OnPhaseStart" Native trigger output.
---@field Phase "Phase" Native data output.
---@field OnSuccessEvent "OnSuccessEvent" Native trigger output.
---@field OnFailEvent "OnFailEvent" Native trigger output.
---@field OnPhaseSkipped "OnPhaseSkipped" Native trigger output.
---@field Tracker "Tracker" Native data output.

---@alias wh.combatmodule.CombatTutorialNode.TriggerName
---| "CustomStepEvent"

---@class wh.combatmodule.CombatTutorialNode.Triggers
---@field CustomStepEvent "CustomStepEvent" Native action trigger input.

---@class wh.combatmodule.CombatTutorialNode
---@field private __skald_handle integer?
---@field private __skald_outputs table?
---@field Outputs wh.combatmodule.CombatTutorialNode.Outputs
---@field Triggers wh.combatmodule.CombatTutorialNode.Triggers

if rawget(Class, "Outputs") ~= nil then
    error("[LuaUtils] public symbol collision at wh.combatmodule.CombatTutorialNode.Outputs")
end
local __outputValues = { OnTutorialEnd = "OnTutorialEnd", OnPhaseStart = "OnPhaseStart", Phase = "Phase", OnSuccessEvent = "OnSuccessEvent", OnFailEvent = "OnFailEvent", OnPhaseSkipped = "OnPhaseSkipped", Tracker = "Tracker" }
Class.Outputs = readonly(__outputValues, "wh.combatmodule.CombatTutorialNode.Outputs")
local __outputSpecs = {
    ["OnTutorialEnd"] = { kind = "trigger", name = "OnTutorialEnd", wrap = nil },
    ["OnPhaseStart"] = { kind = "trigger", name = "OnPhaseStart", wrap = nil },
    ["Phase"] = { kind = "data", name = "Phase", wrap = nil },
    ["OnSuccessEvent"] = { kind = "trigger", name = "OnSuccessEvent", wrap = nil },
    ["OnFailEvent"] = { kind = "trigger", name = "OnFailEvent", wrap = nil },
    ["OnPhaseSkipped"] = { kind = "trigger", name = "OnPhaseSkipped", wrap = nil },
    ["Tracker"] = { kind = "data", name = "Tracker", wrap = wh.questmodule.nodes.Tracker.__from_handle },
}

if rawget(Class, "Triggers") ~= nil then
    error("[LuaUtils] public symbol collision at wh.combatmodule.CombatTutorialNode.Triggers")
end
local __triggerValues = { CustomStepEvent = "CustomStepEvent" }
Class.Triggers = readonly(__triggerValues, "wh.combatmodule.CombatTutorialNode.Triggers")
local __triggerSpecs = {
    ["CustomStepEvent"] = "CustomStepEvent",
}

local __createInputs = {
    ["IsActive"] = true,
    ["Tutor"] = true,
    ["Domain"] = true,
    ["InitialPhase"] = true,
}
local __requiredInputs = {
}
local __arrayInputs = {
}

if rawget(Class, "Create") ~= nil then
    error("[LuaUtils] public symbol collision at wh.combatmodule.CombatTutorialNode.Create")
end
---@param args wh.combatmodule.CombatTutorialNode.CreateArgs
---@return wh.combatmodule.CombatTutorialNode? node
---@return string? error
function Class.Create(args)
    if type(args) ~= "table" then
        return nil, "wh.combatmodule.CombatTutorialNode.Create expects an argument table"
    end
    for name in pairs(args) do
        if not __createInputs[name] then
            return nil, "unknown creation input '" .. tostring(name) ..
                "' for wh.combatmodule.CombatTutorialNode"
        end
    end
    for name in pairs(__requiredInputs) do
        if rawget(args, name) == nil then
            return nil, "missing required creation input '" .. name ..
                "' for wh.combatmodule.CombatTutorialNode"
        end
    end
    local nativeArgs = {}
    local metaArgs = {}
    for name, value in pairs(args) do
        if not metaArgs[name] then
            local arrayAlias = __arrayInputs[name]
            if arrayAlias and type(value) == "table" and
                    value.__rttr_handle == nil then
                local arrayValue = { __array_type = arrayAlias }
                for i, element in ipairs(value) do
                    if type(element) == "table" and
                        element.__rttr_handle ~= nil then
                        arrayValue[i] = element.__rttr_handle
                    else
                        arrayValue[i] = element
                    end
                end
                nativeArgs[name] = arrayValue
            elseif type(value) == "table" and value.__rttr_handle ~= nil then
                nativeArgs[name] = value.__rttr_handle
            else
                nativeArgs[name] = value
            end
        end
    end
    local skaldHandle, rttrHandleOrError =
        raw_skald.CreateNode("CombatTutorialNode", nativeArgs)
    if skaldHandle == nil then return nil, rttrHandleOrError end
    local object = Class.__from_handle(rttrHandleOrError)
    object.__skald_handle = skaldHandle
    object.__skald_outputs = {}
    local registered, registerError = __registerNode(object)
    if not registered then
        raw_skald.DestroyNode(skaldHandle)
        return nil, registerError
    end
    local sentinel = newproxy(true)
    getmetatable(sentinel).__gc = function()
        if rawget(object, "__skald_handle") ~= nil then
            raw_skald.QueueDestroy(skaldHandle)
        end
    end
    object.__skald_gc = sentinel
    return object
end

---@overload fun(self: wh.combatmodule.CombatTutorialNode, output: "OnTutorialEnd", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.combatmodule.CombatTutorialNode, output: "OnPhaseStart", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.combatmodule.CombatTutorialNode, output: "Phase"): Skald.OutputValue<any>?, string?
---@overload fun(self: wh.combatmodule.CombatTutorialNode, output: "OnSuccessEvent", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.combatmodule.CombatTutorialNode, output: "OnFailEvent", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.combatmodule.CombatTutorialNode, output: "OnPhaseSkipped", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.combatmodule.CombatTutorialNode, output: "Tracker"): Skald.OutputValue<wh.questmodule.nodes.Tracker>?, string?
if rawget(Class, "BindOutput") ~= nil then
    error("[LuaUtils] public symbol collision at wh.combatmodule.CombatTutorialNode.BindOutput")
end
function Class:BindOutput(output, callback)
    if self.__skald_handle == nil then
        return nil, "wh.combatmodule.CombatTutorialNode is not a live LuaUtils-created node"
    end
    local spec = __outputSpecs[output]
    if spec == nil then
        return nil, "unknown output selector for wh.combatmodule.CombatTutorialNode"
    end
    local outputs = rawget(self, "__skald_outputs")
    if type(outputs) ~= "table" then
        return nil, "SKALD output registry is unavailable"
    end
    if spec.kind == "trigger" then
        if type(callback) ~= "function" then
            return nil, "trigger output '" .. spec.name ..
                "' requires a callback"
        end
        local handle, err = raw_skald.BindTriggerOutput(
            self.__skald_handle, spec.name, callback)
        if handle == nil then return nil, err end
        local connection = Skald.OutputConnection.__new(handle)
        outputs[#outputs + 1] = connection
        return connection
    end
    if callback ~= nil then
        return nil, "data output '" .. spec.name ..
            "' does not accept a callback"
    end
    local handle, valueOrError = raw_skald.BindDataOutput(
        self.__skald_handle, spec.name)
    if handle == nil then return nil, valueOrError end
    local holder = Skald.OutputValue.__new(
        handle, valueOrError, spec.wrap)
    outputs[#outputs + 1] = holder
    return holder
end

if rawget(Class, "TriggerInput") ~= nil then
    error("[LuaUtils] public symbol collision at wh.combatmodule.CombatTutorialNode.TriggerInput")
end
---@param trigger wh.combatmodule.CombatTutorialNode.TriggerName
---@return boolean? ok
---@return string? error
function Class:TriggerInput(trigger)
    if self.__skald_handle == nil then
        return nil, "wh.combatmodule.CombatTutorialNode is not a live LuaUtils-created node"
    end
    local name = __triggerSpecs[trigger]
    if name == nil then
        return nil, "unknown trigger selector for wh.combatmodule.CombatTutorialNode"
    end
    local updates, err = raw_skald.TriggerInput(
        self.__skald_handle, name)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Activate") ~= nil then
    error("[LuaUtils] public symbol collision at wh.combatmodule.CombatTutorialNode.Activate")
end
---@return boolean? ok
---@return string? error
function Class:Activate()
    if self.__skald_handle == nil then
        return nil, "wh.combatmodule.CombatTutorialNode is not a live LuaUtils-created node"
    end
    local updates, err = raw_skald.SetActivation(
        self.__skald_handle, true)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Deactivate") ~= nil then
    error("[LuaUtils] public symbol collision at wh.combatmodule.CombatTutorialNode.Deactivate")
end
---@return boolean? ok
---@return string? error
function Class:Deactivate()
    if self.__skald_handle == nil then
        return nil, "wh.combatmodule.CombatTutorialNode is not a live LuaUtils-created node"
    end
    local updates, err = raw_skald.SetActivation(
        self.__skald_handle, false)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Destroy") ~= nil then
    error("[LuaUtils] public symbol collision at wh.combatmodule.CombatTutorialNode.Destroy")
end
---@return boolean? ok
---@return string? error
function Class:Destroy()
    if self.__skald_handle == nil then
        return nil, "wh.combatmodule.CombatTutorialNode is not a live LuaUtils-created node"
    end
    local ok, err = raw_skald.DestroyNode(self.__skald_handle)
    if ok then __invalidateNode(self) end
    return ok, err
end

local __rttrRelease = Class.Release
function Class:Release()
    if self.__skald_handle ~= nil then return self:Destroy() end
    return __rttrRelease(self)
end
Class.__skald_generated_node = "wh::combatmodule::C_CombatTutorialNode"

return Class
