-- generated by generate_skald_lua.py; do not edit by hand
-- SKALD augmentation for wh::guimodule::C_CutsceneHandler
local raw_skald = SKALD
if type(raw_skald) ~= "table" then
    error("[LuaUtils] SKALD node wh::guimodule::C_CutsceneHandler: native SKALD table missing")
end
local Class = wh.guimodule.CutsceneHandler
if type(Class) ~= "table" or Class.__cpp_name ~= "wh::guimodule::C_CutsceneHandler" then
    error("[LuaUtils] SKALD class identity mismatch at wh.guimodule.CutsceneHandler")
end
local generated = rawget(Class, "__skald_generated_node")
if generated ~= nil then
    if generated ~= "wh::guimodule::C_CutsceneHandler" then
        error("[LuaUtils] incompatible generated SKALD node at wh.guimodule.CutsceneHandler")
    end
    return Class
end
local __applyUpdates = Skald.__applyUpdates
if type(__applyUpdates) ~= "function" then
    error("[LuaUtils] SKALD update dispatcher missing")
end
local __registerNode = Skald.__registerNode
if type(__registerNode) ~= "function" then
    error("[LuaUtils] SKALD node registrar missing")
end
local __invalidateNode = Skald.__invalidateNode
if type(__invalidateNode) ~= "function" then
    error("[LuaUtils] SKALD node invalidator missing")
end
local function readonly(values, label)
    return setmetatable({}, {
        __index = values,
        __newindex = function()
            error(label .. " is read-only", 2)
        end,
        __metatable = false,
    })
end

---@class wh.guimodule.CutsceneHandler.CreateArgs
---@field CutsceneHolder userdata? Native CutsceneHolder object input (RTTR handle or wrapper).
---@field AutoPlay boolean? Native AutoPlay data input.
---@field AutoFinish boolean? Native AutoFinish data input.

---@alias wh.guimodule.CutsceneHandler.OutputName
---| "OnQueued"
---| "BeforePlay"
---| "AfterPlay"
---| "OnFinished"

---@class wh.guimodule.CutsceneHandler.Outputs
---@field OnQueued "OnQueued" Native trigger output.
---@field BeforePlay "BeforePlay" Native trigger output.
---@field AfterPlay "AfterPlay" Native trigger output.
---@field OnFinished "OnFinished" Native trigger output.

---@alias wh.guimodule.CutsceneHandler.TriggerName
---| "EnqueueCutscene"
---| "PlayCutscene"
---| "FinishCutscene"

---@class wh.guimodule.CutsceneHandler.Triggers
---@field EnqueueCutscene "EnqueueCutscene" Native action trigger input.
---@field PlayCutscene "PlayCutscene" Native action trigger input.
---@field FinishCutscene "FinishCutscene" Native action trigger input.

---@class wh.guimodule.CutsceneHandler
---@field private __skald_handle integer?
---@field private __skald_outputs table?
---@field Outputs wh.guimodule.CutsceneHandler.Outputs
---@field Triggers wh.guimodule.CutsceneHandler.Triggers

if rawget(Class, "Outputs") ~= nil then
    error("[LuaUtils] public symbol collision at wh.guimodule.CutsceneHandler.Outputs")
end
local __outputValues = { OnQueued = "OnQueued", BeforePlay = "BeforePlay", AfterPlay = "AfterPlay", OnFinished = "OnFinished" }
Class.Outputs = readonly(__outputValues, "wh.guimodule.CutsceneHandler.Outputs")
local __outputSpecs = {
    ["OnQueued"] = { kind = "trigger", name = "OnQueued", wrap = nil },
    ["BeforePlay"] = { kind = "trigger", name = "BeforePlay", wrap = nil },
    ["AfterPlay"] = { kind = "trigger", name = "AfterPlay", wrap = nil },
    ["OnFinished"] = { kind = "trigger", name = "OnFinished", wrap = nil },
}

if rawget(Class, "Triggers") ~= nil then
    error("[LuaUtils] public symbol collision at wh.guimodule.CutsceneHandler.Triggers")
end
local __triggerValues = { EnqueueCutscene = "EnqueueCutscene", PlayCutscene = "PlayCutscene", FinishCutscene = "FinishCutscene" }
Class.Triggers = readonly(__triggerValues, "wh.guimodule.CutsceneHandler.Triggers")
local __triggerSpecs = {
    ["EnqueueCutscene"] = "EnqueueCutscene",
    ["PlayCutscene"] = "PlayCutscene",
    ["FinishCutscene"] = "FinishCutscene",
}

local __createInputs = {
    ["CutsceneHolder"] = true,
    ["AutoPlay"] = true,
    ["AutoFinish"] = true,
}
local __requiredInputs = {
}
local __arrayInputs = {
}

if rawget(Class, "Create") ~= nil then
    error("[LuaUtils] public symbol collision at wh.guimodule.CutsceneHandler.Create")
end
---@param args wh.guimodule.CutsceneHandler.CreateArgs
---@return wh.guimodule.CutsceneHandler? node
---@return string? error
function Class.Create(args)
    if type(args) ~= "table" then
        return nil, "wh.guimodule.CutsceneHandler.Create expects an argument table"
    end
    for name in pairs(args) do
        if not __createInputs[name] then
            return nil, "unknown creation input '" .. tostring(name) ..
                "' for wh.guimodule.CutsceneHandler"
        end
    end
    for name in pairs(__requiredInputs) do
        if rawget(args, name) == nil then
            return nil, "missing required creation input '" .. name ..
                "' for wh.guimodule.CutsceneHandler"
        end
    end
    local nativeArgs = {}
    local metaArgs = {}
    for name, value in pairs(args) do
        if not metaArgs[name] then
            local arrayAlias = __arrayInputs[name]
            if arrayAlias and type(value) == "table" and
                    value.__rttr_handle == nil then
                local arrayValue = { __array_type = arrayAlias }
                for i, element in ipairs(value) do
                    if type(element) == "table" and
                        element.__rttr_handle ~= nil then
                        arrayValue[i] = element.__rttr_handle
                    else
                        arrayValue[i] = element
                    end
                end
                nativeArgs[name] = arrayValue
            elseif type(value) == "table" and value.__rttr_handle ~= nil then
                nativeArgs[name] = value.__rttr_handle
            else
                nativeArgs[name] = value
            end
        end
    end
    local skaldHandle, rttrHandleOrError =
        raw_skald.CreateNode("CutsceneHandler", nativeArgs)
    if skaldHandle == nil then return nil, rttrHandleOrError end
    local object = Class.__from_handle(rttrHandleOrError)
    object.__skald_handle = skaldHandle
    object.__skald_outputs = {}
    local registered, registerError = __registerNode(object)
    if not registered then
        raw_skald.DestroyNode(skaldHandle)
        return nil, registerError
    end
    local sentinel = newproxy(true)
    getmetatable(sentinel).__gc = function()
        if rawget(object, "__skald_handle") ~= nil then
            raw_skald.QueueDestroy(skaldHandle)
        end
    end
    object.__skald_gc = sentinel
    return object
end

---@overload fun(self: wh.guimodule.CutsceneHandler, output: "OnQueued", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.guimodule.CutsceneHandler, output: "BeforePlay", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.guimodule.CutsceneHandler, output: "AfterPlay", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.guimodule.CutsceneHandler, output: "OnFinished", callback: fun()): Skald.OutputConnection?, string?
if rawget(Class, "BindOutput") ~= nil then
    error("[LuaUtils] public symbol collision at wh.guimodule.CutsceneHandler.BindOutput")
end
function Class:BindOutput(output, callback)
    if self.__skald_handle == nil then
        return nil, "wh.guimodule.CutsceneHandler is not a live LuaUtils-created node"
    end
    local spec = __outputSpecs[output]
    if spec == nil then
        return nil, "unknown output selector for wh.guimodule.CutsceneHandler"
    end
    local outputs = rawget(self, "__skald_outputs")
    if type(outputs) ~= "table" then
        return nil, "SKALD output registry is unavailable"
    end
    if spec.kind == "trigger" then
        if type(callback) ~= "function" then
            return nil, "trigger output '" .. spec.name ..
                "' requires a callback"
        end
        local handle, err = raw_skald.BindTriggerOutput(
            self.__skald_handle, spec.name, callback)
        if handle == nil then return nil, err end
        local connection = Skald.OutputConnection.__new(handle)
        outputs[#outputs + 1] = connection
        return connection
    end
    if callback ~= nil then
        return nil, "data output '" .. spec.name ..
            "' does not accept a callback"
    end
    local handle, valueOrError = raw_skald.BindDataOutput(
        self.__skald_handle, spec.name)
    if handle == nil then return nil, valueOrError end
    local holder = Skald.OutputValue.__new(
        handle, valueOrError, spec.wrap)
    outputs[#outputs + 1] = holder
    return holder
end

if rawget(Class, "TriggerInput") ~= nil then
    error("[LuaUtils] public symbol collision at wh.guimodule.CutsceneHandler.TriggerInput")
end
---@param trigger wh.guimodule.CutsceneHandler.TriggerName
---@return boolean? ok
---@return string? error
function Class:TriggerInput(trigger)
    if self.__skald_handle == nil then
        return nil, "wh.guimodule.CutsceneHandler is not a live LuaUtils-created node"
    end
    local name = __triggerSpecs[trigger]
    if name == nil then
        return nil, "unknown trigger selector for wh.guimodule.CutsceneHandler"
    end
    local updates, err = raw_skald.TriggerInput(
        self.__skald_handle, name)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Destroy") ~= nil then
    error("[LuaUtils] public symbol collision at wh.guimodule.CutsceneHandler.Destroy")
end
---@return boolean? ok
---@return string? error
function Class:Destroy()
    if self.__skald_handle == nil then
        return nil, "wh.guimodule.CutsceneHandler is not a live LuaUtils-created node"
    end
    local ok, err = raw_skald.DestroyNode(self.__skald_handle)
    if ok then __invalidateNode(self) end
    return ok, err
end

local __rttrRelease = Class.Release
function Class:Release()
    if self.__skald_handle ~= nil then return self:Destroy() end
    return __rttrRelease(self)
end
Class.__skald_generated_node = "wh::guimodule::C_CutsceneHandler"

return Class
