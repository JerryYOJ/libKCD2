-- generated by KCD2/analysis/luautils/tools/generate_skald_lua.py; do not edit by hand
-- SKALD augmentation for wh::rpgmodule::C_Timer
local raw_skald = SKALD
if type(raw_skald) ~= "table" then
    error("[LuaUtils] SKALD node wh::rpgmodule::C_Timer: native SKALD table missing")
end
local Class = wh.rpgmodule.Timer
if type(Class) ~= "table" or Class.__cpp_name ~= "wh::rpgmodule::C_Timer" then
    error("[LuaUtils] SKALD class identity mismatch at wh.rpgmodule.Timer")
end
local generated = rawget(Class, "__skald_generated_node")
if generated ~= nil then
    if generated ~= "wh::rpgmodule::C_Timer" then
        error("[LuaUtils] incompatible generated SKALD node at wh.rpgmodule.Timer")
    end
    return Class
end
local __applyUpdates = Skald.__applyUpdates
if type(__applyUpdates) ~= "function" then
    error("[LuaUtils] SKALD update dispatcher missing")
end
local __registerNode = Skald.__registerNode
if type(__registerNode) ~= "function" then
    error("[LuaUtils] SKALD node registrar missing")
end
local __invalidateNode = Skald.__invalidateNode
if type(__invalidateNode) ~= "function" then
    error("[LuaUtils] SKALD node invalidator missing")
end
local function readonly(values, label)
    return setmetatable({}, {
        __index = values,
        __newindex = function()
            error(label .. " is read-only", 2)
        end,
        __metatable = false,
    })
end

---@class wh.rpgmodule.Timer.CreateArgs
---@field TypeT string|table? Certified type parameter ('Strings'; RTTR class tables accepted). Defaults to 'Strings'.
---@field Duration userdata? Native Duration object input (RTTR handle or wrapper).
---@field TimeType userdata? Native TimeType object input (RTTR handle or wrapper).

---@alias wh.rpgmodule.Timer.OutputName
---| "State"
---| "OnExec"
---| "OnStopped"
---| "Stopped"
---| "OnRunning"
---| "Running"
---| "OnFinished"
---| "Finished"

---@class wh.rpgmodule.Timer.Outputs
---@field State "State" Native data output.
---@field OnExec "OnExec" Native trigger output.
---@field OnStopped "OnStopped" Native trigger output.
---@field Stopped "Stopped" Native data output.
---@field OnRunning "OnRunning" Native trigger output.
---@field Running "Running" Native data output.
---@field OnFinished "OnFinished" Native trigger output.
---@field Finished "Finished" Native data output.

---@alias wh.rpgmodule.Timer.TriggerName
---| "SetStopped"
---| "SetRunning"
---| "SetFinished"

---@class wh.rpgmodule.Timer.Triggers
---@field SetStopped "SetStopped" Native action trigger input.
---@field SetRunning "SetRunning" Native action trigger input.
---@field SetFinished "SetFinished" Native action trigger input.

---@class wh.rpgmodule.Timer
---@field private __skald_handle integer?
---@field private __skald_outputs table?
---@field Outputs wh.rpgmodule.Timer.Outputs
---@field Triggers wh.rpgmodule.Timer.Triggers

if rawget(Class, "Outputs") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.Timer.Outputs")
end
local __outputValues = { State = "State", OnExec = "OnExec", OnStopped = "OnStopped", Stopped = "Stopped", OnRunning = "OnRunning", Running = "Running", OnFinished = "OnFinished", Finished = "Finished" }
Class.Outputs = readonly(__outputValues, "wh.rpgmodule.Timer.Outputs")
local __outputSpecs = {
    ["State"] = { kind = "data", name = "State", wrap = wh.rpgmodule.TimerState.__from_handle },
    ["OnExec"] = { kind = "trigger", name = "OnExec", wrap = nil },
    ["OnStopped"] = { kind = "trigger", name = "OnStopped", wrap = nil },
    ["Stopped"] = { kind = "data", name = "Stopped", wrap = nil },
    ["OnRunning"] = { kind = "trigger", name = "OnRunning", wrap = nil },
    ["Running"] = { kind = "data", name = "Running", wrap = nil },
    ["OnFinished"] = { kind = "trigger", name = "OnFinished", wrap = nil },
    ["Finished"] = { kind = "data", name = "Finished", wrap = nil },
}

if rawget(Class, "Triggers") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.Timer.Triggers")
end
local __triggerValues = { SetStopped = "SetStopped", SetRunning = "SetRunning", SetFinished = "SetFinished" }
Class.Triggers = readonly(__triggerValues, "wh.rpgmodule.Timer.Triggers")
local __triggerSpecs = {
    ["SetStopped"] = "SetStopped",
    ["SetRunning"] = "SetRunning",
    ["SetFinished"] = "SetFinished",
}

local __createInputs = {
    ["TypeT"] = true,
    ["Duration"] = true,
    ["TimeType"] = true,
}
local __requiredInputs = {
}
local __arrayInputs = {
}
local __typeParameterSet = { ["Strings"] = true }
local __typeParameterList = "'Strings'"
local __typeAliases = {}
local __defaultTypeT = "Strings"

if rawget(Class, "Create") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.Timer.Create")
end
---@param args wh.rpgmodule.Timer.CreateArgs
---@return wh.rpgmodule.Timer? node
---@return string? error
function Class.Create(args)
    if type(args) ~= "table" then
        return nil, "wh.rpgmodule.Timer.Create expects an argument table"
    end
    for name in pairs(args) do
        if not __createInputs[name] then
            return nil, "unknown creation input '" .. tostring(name) ..
                "' for wh.rpgmodule.Timer"
        end
    end
    for name in pairs(__requiredInputs) do
        if rawget(args, name) == nil then
            return nil, "missing required creation input '" .. name ..
                "' for wh.rpgmodule.Timer"
        end
    end
    local nativeArgs = {}
    local metaArgs = {}
    metaArgs["TypeT"] = true
    local typeT = rawget(args, "TypeT")
    if typeT == nil then typeT = __defaultTypeT end
    if type(typeT) == "table" then
        typeT = __typeAliases[typeT.__cpp_name]
    end
    if type(typeT) ~= "string" or not __typeParameterSet[typeT] then
        return nil, "TypeT must be one of " .. __typeParameterList ..
            " for wh.rpgmodule.Timer"
    end
    nativeArgs["TypeT"] = typeT
    for name, value in pairs(args) do
        if not metaArgs[name] then
            local arrayAlias = __arrayInputs[name]
            if arrayAlias and type(value) == "table" and
                    value.__rttr_handle == nil then
                local arrayValue = { __array_type = arrayAlias }
                for i, element in ipairs(value) do
                    if type(element) == "table" and
                        element.__rttr_handle ~= nil then
                        arrayValue[i] = element.__rttr_handle
                    else
                        arrayValue[i] = element
                    end
                end
                nativeArgs[name] = arrayValue
            elseif type(value) == "table" and value.__rttr_handle ~= nil then
                nativeArgs[name] = value.__rttr_handle
            else
                nativeArgs[name] = value
            end
        end
    end
    local skaldHandle, rttrHandleOrError =
        raw_skald.CreateNode("wh::rpgmodule::Timer", nativeArgs)
    if skaldHandle == nil then return nil, rttrHandleOrError end
    local object = Class.__from_handle(rttrHandleOrError)
    object.__skald_handle = skaldHandle
    object.__skald_outputs = {}
    local registered, registerError = __registerNode(object)
    if not registered then
        raw_skald.DestroyNode(skaldHandle)
        return nil, registerError
    end
    local sentinel = newproxy(true)
    getmetatable(sentinel).__gc = function()
        if rawget(object, "__skald_handle") ~= nil then
            raw_skald.QueueDestroy(skaldHandle)
        end
    end
    object.__skald_gc = sentinel
    return object
end

---@overload fun(self: wh.rpgmodule.Timer, output: "State"): Skald.OutputValue<wh.rpgmodule.TimerState>?, string?
---@overload fun(self: wh.rpgmodule.Timer, output: "OnExec", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.rpgmodule.Timer, output: "OnStopped", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.rpgmodule.Timer, output: "Stopped"): Skald.OutputValue<any>?, string?
---@overload fun(self: wh.rpgmodule.Timer, output: "OnRunning", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.rpgmodule.Timer, output: "Running"): Skald.OutputValue<any>?, string?
---@overload fun(self: wh.rpgmodule.Timer, output: "OnFinished", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.rpgmodule.Timer, output: "Finished"): Skald.OutputValue<any>?, string?
if rawget(Class, "BindOutput") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.Timer.BindOutput")
end
function Class:BindOutput(output, callback)
    if self.__skald_handle == nil then
        return nil, "wh.rpgmodule.Timer is not a live LuaUtils-created node"
    end
    local spec = __outputSpecs[output]
    if spec == nil then
        return nil, "unknown output selector for wh.rpgmodule.Timer"
    end
    local outputs = rawget(self, "__skald_outputs")
    if type(outputs) ~= "table" then
        return nil, "SKALD output registry is unavailable"
    end
    if spec.kind == "trigger" then
        if type(callback) ~= "function" then
            return nil, "trigger output '" .. spec.name ..
                "' requires a callback"
        end
        local handle, err = raw_skald.BindTriggerOutput(
            self.__skald_handle, spec.name, callback)
        if handle == nil then return nil, err end
        local connection = Skald.OutputConnection.__new(handle)
        outputs[#outputs + 1] = connection
        return connection
    end
    if callback ~= nil then
        return nil, "data output '" .. spec.name ..
            "' does not accept a callback"
    end
    local handle, valueOrError = raw_skald.BindDataOutput(
        self.__skald_handle, spec.name)
    if handle == nil then return nil, valueOrError end
    local holder = Skald.OutputValue.__new(
        handle, valueOrError, spec.wrap)
    outputs[#outputs + 1] = holder
    return holder
end

if rawget(Class, "TriggerInput") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.Timer.TriggerInput")
end
---@param trigger wh.rpgmodule.Timer.TriggerName
---@return boolean? ok
---@return string? error
function Class:TriggerInput(trigger)
    if self.__skald_handle == nil then
        return nil, "wh.rpgmodule.Timer is not a live LuaUtils-created node"
    end
    local name = __triggerSpecs[trigger]
    if name == nil then
        return nil, "unknown trigger selector for wh.rpgmodule.Timer"
    end
    local updates, err = raw_skald.TriggerInput(
        self.__skald_handle, name)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Destroy") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.Timer.Destroy")
end
---@return boolean? ok
---@return string? error
function Class:Destroy()
    if self.__skald_handle == nil then
        return nil, "wh.rpgmodule.Timer is not a live LuaUtils-created node"
    end
    local ok, err = raw_skald.DestroyNode(self.__skald_handle)
    if ok then __invalidateNode(self) end
    return ok, err
end

local __rttrRelease = Class.Release
function Class:Release()
    if self.__skald_handle ~= nil then return self:Destroy() end
    return __rttrRelease(self)
end
Class.__skald_generated_node = "wh::rpgmodule::C_Timer"

return Class
