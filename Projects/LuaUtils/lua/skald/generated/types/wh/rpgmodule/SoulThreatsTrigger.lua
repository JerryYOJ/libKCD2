-- generated by generate_skald_lua.py; do not edit by hand
-- SKALD augmentation for wh::rpgmodule::C_SoulThreatsTrigger
local raw_skald = SKALD
if type(raw_skald) ~= "table" then
    error("[LuaUtils] SKALD node wh::rpgmodule::C_SoulThreatsTrigger: native SKALD table missing")
end
local Class = wh.rpgmodule.SoulThreatsTrigger
if type(Class) ~= "table" or Class.__cpp_name ~= "wh::rpgmodule::C_SoulThreatsTrigger" then
    error("[LuaUtils] SKALD class identity mismatch at wh.rpgmodule.SoulThreatsTrigger")
end
local generated = rawget(Class, "__skald_generated_node")
if generated ~= nil then
    if generated ~= "wh::rpgmodule::C_SoulThreatsTrigger" then
        error("[LuaUtils] incompatible generated SKALD node at wh.rpgmodule.SoulThreatsTrigger")
    end
    return Class
end
local __applyUpdates = Skald.__applyUpdates
if type(__applyUpdates) ~= "function" then
    error("[LuaUtils] SKALD update dispatcher missing")
end
local __registerNode = Skald.__registerNode
if type(__registerNode) ~= "function" then
    error("[LuaUtils] SKALD node registrar missing")
end
local __invalidateNode = Skald.__invalidateNode
if type(__invalidateNode) ~= "function" then
    error("[LuaUtils] SKALD node invalidator missing")
end
local function readonly(values, label)
    return setmetatable({}, {
        __index = values,
        __newindex = function()
            error(label .. " is read-only", 2)
        end,
        __metatable = false,
    })
end

---@class wh.rpgmodule.SoulThreatsTrigger.CreateArgs
---@field IsActive boolean? Native IsActive data input.
---@field Souls table? Native Souls input: a plain array (table) of RTTR handles/wrappers, or an array handle.

---@alias wh.rpgmodule.SoulThreatsTrigger.OutputName
---| "Soul"
---| "Tracker"
---| "OnAllThreatsActive"
---| "OnThreatsActive"
---| "OnAllThreatsInactive"
---| "OnThreatsInactive"

---@class wh.rpgmodule.SoulThreatsTrigger.Outputs
---@field Soul "Soul" Native data output.
---@field Tracker "Tracker" Native data output.
---@field OnAllThreatsActive "OnAllThreatsActive" Native trigger output.
---@field OnThreatsActive "OnThreatsActive" Native trigger output.
---@field OnAllThreatsInactive "OnAllThreatsInactive" Native trigger output.
---@field OnThreatsInactive "OnThreatsInactive" Native trigger output.

---@class wh.rpgmodule.SoulThreatsTrigger
---@field private __skald_handle integer?
---@field private __skald_outputs table?
---@field Outputs wh.rpgmodule.SoulThreatsTrigger.Outputs

if rawget(Class, "Outputs") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.SoulThreatsTrigger.Outputs")
end
local __outputValues = { Soul = "Soul", Tracker = "Tracker", OnAllThreatsActive = "OnAllThreatsActive", OnThreatsActive = "OnThreatsActive", OnAllThreatsInactive = "OnAllThreatsInactive", OnThreatsInactive = "OnThreatsInactive" }
Class.Outputs = readonly(__outputValues, "wh.rpgmodule.SoulThreatsTrigger.Outputs")
local __outputSpecs = {
    ["Soul"] = { kind = "data", name = "Soul", wrap = wh.rpgmodule.I_Soul.__from_handle },
    ["Tracker"] = { kind = "data", name = "Tracker", wrap = wh.questmodule.nodes.Tracker.__from_handle },
    ["OnAllThreatsActive"] = { kind = "trigger", name = "OnAllThreatsActive", wrap = nil },
    ["OnThreatsActive"] = { kind = "trigger", name = "OnThreatsActive", wrap = nil },
    ["OnAllThreatsInactive"] = { kind = "trigger", name = "OnAllThreatsInactive", wrap = nil },
    ["OnThreatsInactive"] = { kind = "trigger", name = "OnThreatsInactive", wrap = nil },
}

local __createInputs = {
    ["IsActive"] = true,
    ["Souls"] = true,
}
local __requiredInputs = {
}
local __arrayInputs = {
    ["Souls"] = "wh::rpgmodule::Souls",
}

if rawget(Class, "Create") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.SoulThreatsTrigger.Create")
end
---@param args wh.rpgmodule.SoulThreatsTrigger.CreateArgs
---@return wh.rpgmodule.SoulThreatsTrigger? node
---@return string? error
function Class.Create(args)
    if type(args) ~= "table" then
        return nil, "wh.rpgmodule.SoulThreatsTrigger.Create expects an argument table"
    end
    for name in pairs(args) do
        if not __createInputs[name] then
            return nil, "unknown creation input '" .. tostring(name) ..
                "' for wh.rpgmodule.SoulThreatsTrigger"
        end
    end
    for name in pairs(__requiredInputs) do
        if rawget(args, name) == nil then
            return nil, "missing required creation input '" .. name ..
                "' for wh.rpgmodule.SoulThreatsTrigger"
        end
    end
    local nativeArgs = {}
    local metaArgs = {}
    for name, value in pairs(args) do
        if not metaArgs[name] then
            local arrayAlias = __arrayInputs[name]
            if arrayAlias and type(value) == "table" and
                    value.__rttr_handle == nil then
                local arrayValue = { __array_type = arrayAlias }
                for i, element in ipairs(value) do
                    if type(element) == "table" and
                        element.__rttr_handle ~= nil then
                        arrayValue[i] = element.__rttr_handle
                    else
                        arrayValue[i] = element
                    end
                end
                nativeArgs[name] = arrayValue
            elseif type(value) == "table" and value.__rttr_handle ~= nil then
                nativeArgs[name] = value.__rttr_handle
            else
                nativeArgs[name] = value
            end
        end
    end
    local skaldHandle, rttrHandleOrError =
        raw_skald.CreateNode("SoulThreatsTrigger", nativeArgs)
    if skaldHandle == nil then return nil, rttrHandleOrError end
    local object = Class.__from_handle(rttrHandleOrError)
    object.__skald_handle = skaldHandle
    object.__skald_outputs = {}
    local registered, registerError = __registerNode(object)
    if not registered then
        raw_skald.DestroyNode(skaldHandle)
        return nil, registerError
    end
    local sentinel = newproxy(true)
    getmetatable(sentinel).__gc = function()
        if rawget(object, "__skald_handle") ~= nil then
            raw_skald.QueueDestroy(skaldHandle)
        end
    end
    object.__skald_gc = sentinel
    return object
end

---@overload fun(self: wh.rpgmodule.SoulThreatsTrigger, output: "Soul"): Skald.OutputValue<wh.rpgmodule.I_Soul>?, string?
---@overload fun(self: wh.rpgmodule.SoulThreatsTrigger, output: "Tracker"): Skald.OutputValue<wh.questmodule.nodes.Tracker>?, string?
---@overload fun(self: wh.rpgmodule.SoulThreatsTrigger, output: "OnAllThreatsActive", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.rpgmodule.SoulThreatsTrigger, output: "OnThreatsActive", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.rpgmodule.SoulThreatsTrigger, output: "OnAllThreatsInactive", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.rpgmodule.SoulThreatsTrigger, output: "OnThreatsInactive", callback: fun()): Skald.OutputConnection?, string?
if rawget(Class, "BindOutput") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.SoulThreatsTrigger.BindOutput")
end
function Class:BindOutput(output, callback)
    if self.__skald_handle == nil then
        return nil, "wh.rpgmodule.SoulThreatsTrigger is not a live LuaUtils-created node"
    end
    local spec = __outputSpecs[output]
    if spec == nil then
        return nil, "unknown output selector for wh.rpgmodule.SoulThreatsTrigger"
    end
    local outputs = rawget(self, "__skald_outputs")
    if type(outputs) ~= "table" then
        return nil, "SKALD output registry is unavailable"
    end
    if spec.kind == "trigger" then
        if type(callback) ~= "function" then
            return nil, "trigger output '" .. spec.name ..
                "' requires a callback"
        end
        local handle, err = raw_skald.BindTriggerOutput(
            self.__skald_handle, spec.name, callback)
        if handle == nil then return nil, err end
        local connection = Skald.OutputConnection.__new(handle)
        outputs[#outputs + 1] = connection
        return connection
    end
    if callback ~= nil then
        return nil, "data output '" .. spec.name ..
            "' does not accept a callback"
    end
    local handle, valueOrError = raw_skald.BindDataOutput(
        self.__skald_handle, spec.name)
    if handle == nil then return nil, valueOrError end
    local holder = Skald.OutputValue.__new(
        handle, valueOrError, spec.wrap)
    outputs[#outputs + 1] = holder
    return holder
end

if rawget(Class, "Activate") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.SoulThreatsTrigger.Activate")
end
---@return boolean? ok
---@return string? error
function Class:Activate()
    if self.__skald_handle == nil then
        return nil, "wh.rpgmodule.SoulThreatsTrigger is not a live LuaUtils-created node"
    end
    local updates, err = raw_skald.SetActivation(
        self.__skald_handle, true)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Deactivate") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.SoulThreatsTrigger.Deactivate")
end
---@return boolean? ok
---@return string? error
function Class:Deactivate()
    if self.__skald_handle == nil then
        return nil, "wh.rpgmodule.SoulThreatsTrigger is not a live LuaUtils-created node"
    end
    local updates, err = raw_skald.SetActivation(
        self.__skald_handle, false)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Destroy") ~= nil then
    error("[LuaUtils] public symbol collision at wh.rpgmodule.SoulThreatsTrigger.Destroy")
end
---@return boolean? ok
---@return string? error
function Class:Destroy()
    if self.__skald_handle == nil then
        return nil, "wh.rpgmodule.SoulThreatsTrigger is not a live LuaUtils-created node"
    end
    local ok, err = raw_skald.DestroyNode(self.__skald_handle)
    if ok then __invalidateNode(self) end
    return ok, err
end

local __rttrRelease = Class.Release
function Class:Release()
    if self.__skald_handle ~= nil then return self:Destroy() end
    return __rttrRelease(self)
end
Class.__skald_generated_node = "wh::rpgmodule::C_SoulThreatsTrigger"

return Class
