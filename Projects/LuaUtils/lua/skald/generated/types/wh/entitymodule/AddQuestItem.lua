-- generated by generate_skald_lua.py; do not edit by hand
-- SKALD augmentation for wh::entitymodule::C_AddQuestItem
local raw_skald = SKALD
if type(raw_skald) ~= "table" then
    error("[LuaUtils] SKALD node wh::entitymodule::C_AddQuestItem: native SKALD table missing")
end
local Class = wh.entitymodule.AddQuestItem
if type(Class) ~= "table" or Class.__cpp_name ~= "wh::entitymodule::C_AddQuestItem" then
    error("[LuaUtils] SKALD class identity mismatch at wh.entitymodule.AddQuestItem")
end
local generated = rawget(Class, "__skald_generated_node")
if generated ~= nil then
    if generated ~= "wh::entitymodule::C_AddQuestItem" then
        error("[LuaUtils] incompatible generated SKALD node at wh.entitymodule.AddQuestItem")
    end
    return Class
end
local __applyUpdates = Skald.__applyUpdates
if type(__applyUpdates) ~= "function" then
    error("[LuaUtils] SKALD update dispatcher missing")
end
local __registerNode = Skald.__registerNode
if type(__registerNode) ~= "function" then
    error("[LuaUtils] SKALD node registrar missing")
end
local __invalidateNode = Skald.__invalidateNode
if type(__invalidateNode) ~= "function" then
    error("[LuaUtils] SKALD node invalidator missing")
end
local function readonly(values, label)
    return setmetatable({}, {
        __index = values,
        __newindex = function()
            error(label .. " is read-only", 2)
        end,
        __metatable = false,
    })
end

---@class wh.entitymodule.AddQuestItem.CreateArgs
---@field IsActive boolean? Native IsActive data input.
---@field ItemClassGUID userdata? Native ItemClassGUID object input (RTTR handle or wrapper).
---@field BackupLocation userdata? Native BackupLocation object input (RTTR handle or wrapper).
---@field StartingLocation userdata? Native StartingLocation object input (RTTR handle or wrapper).
---@field Quality integer? Native Quality data input.
---@field Health number? Native Health data input.
---@field Condition number? Native Condition data input.
---@field Blood number? Native Blood data input.
---@field OnEquipBuff userdata? Native OnEquipBuff object input (RTTR handle or wrapper).
---@field DoEquip boolean? Native DoEquip data input.
---@field SuppressUINotificationOnAcquire boolean? Native SuppressUINotificationOnAcquire data input.
---@field SuppressUINotificationOnLost boolean? Native SuppressUINotificationOnLost data input.

---@class wh.entitymodule.AddQuestItem
---@field private __skald_handle integer?
---@field private __skald_outputs table?

local __createInputs = {
    ["IsActive"] = true,
    ["ItemClassGUID"] = true,
    ["BackupLocation"] = true,
    ["StartingLocation"] = true,
    ["Quality"] = true,
    ["Health"] = true,
    ["Condition"] = true,
    ["Blood"] = true,
    ["OnEquipBuff"] = true,
    ["DoEquip"] = true,
    ["SuppressUINotificationOnAcquire"] = true,
    ["SuppressUINotificationOnLost"] = true,
}
local __requiredInputs = {
}
local __arrayInputs = {
}

if rawget(Class, "Create") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.AddQuestItem.Create")
end
---@param args wh.entitymodule.AddQuestItem.CreateArgs
---@return wh.entitymodule.AddQuestItem? node
---@return string? error
function Class.Create(args)
    if type(args) ~= "table" then
        return nil, "wh.entitymodule.AddQuestItem.Create expects an argument table"
    end
    for name in pairs(args) do
        if not __createInputs[name] then
            return nil, "unknown creation input '" .. tostring(name) ..
                "' for wh.entitymodule.AddQuestItem"
        end
    end
    for name in pairs(__requiredInputs) do
        if rawget(args, name) == nil then
            return nil, "missing required creation input '" .. name ..
                "' for wh.entitymodule.AddQuestItem"
        end
    end
    local nativeArgs = {}
    local metaArgs = {}
    for name, value in pairs(args) do
        if not metaArgs[name] then
            local arrayAlias = __arrayInputs[name]
            if arrayAlias and type(value) == "table" and
                    value.__rttr_handle == nil then
                local arrayValue = { __array_type = arrayAlias }
                for i, element in ipairs(value) do
                    if type(element) == "table" and
                        element.__rttr_handle ~= nil then
                        arrayValue[i] = element.__rttr_handle
                    else
                        arrayValue[i] = element
                    end
                end
                nativeArgs[name] = arrayValue
            elseif type(value) == "table" and value.__rttr_handle ~= nil then
                nativeArgs[name] = value.__rttr_handle
            else
                nativeArgs[name] = value
            end
        end
    end
    local skaldHandle, rttrHandleOrError =
        raw_skald.CreateNode("wh::entitymodule::AddQuestItem", nativeArgs)
    if skaldHandle == nil then return nil, rttrHandleOrError end
    local object = Class.__from_handle(rttrHandleOrError)
    object.__skald_handle = skaldHandle
    object.__skald_outputs = {}
    local registered, registerError = __registerNode(object)
    if not registered then
        raw_skald.DestroyNode(skaldHandle)
        return nil, registerError
    end
    local sentinel = newproxy(true)
    getmetatable(sentinel).__gc = function()
        if rawget(object, "__skald_handle") ~= nil then
            raw_skald.QueueDestroy(skaldHandle)
        end
    end
    object.__skald_gc = sentinel
    return object
end

if rawget(Class, "Activate") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.AddQuestItem.Activate")
end
---@return boolean? ok
---@return string? error
function Class:Activate()
    if self.__skald_handle == nil then
        return nil, "wh.entitymodule.AddQuestItem is not a live LuaUtils-created node"
    end
    local updates, err = raw_skald.SetActivation(
        self.__skald_handle, true)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Deactivate") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.AddQuestItem.Deactivate")
end
---@return boolean? ok
---@return string? error
function Class:Deactivate()
    if self.__skald_handle == nil then
        return nil, "wh.entitymodule.AddQuestItem is not a live LuaUtils-created node"
    end
    local updates, err = raw_skald.SetActivation(
        self.__skald_handle, false)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Destroy") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.AddQuestItem.Destroy")
end
---@return boolean? ok
---@return string? error
function Class:Destroy()
    if self.__skald_handle == nil then
        return nil, "wh.entitymodule.AddQuestItem is not a live LuaUtils-created node"
    end
    local ok, err = raw_skald.DestroyNode(self.__skald_handle)
    if ok then __invalidateNode(self) end
    return ok, err
end

local __rttrRelease = Class.Release
function Class:Release()
    if self.__skald_handle ~= nil then return self:Destroy() end
    return __rttrRelease(self)
end
Class.__skald_generated_node = "wh::entitymodule::C_AddQuestItem"

return Class
