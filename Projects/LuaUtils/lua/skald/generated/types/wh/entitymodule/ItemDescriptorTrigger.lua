-- generated by generate_skald_lua.py; do not edit by hand
-- SKALD augmentation for wh::entitymodule::C_ItemDescriptorTrigger
local raw_skald = SKALD
if type(raw_skald) ~= "table" then
    error("[LuaUtils] SKALD node wh::entitymodule::C_ItemDescriptorTrigger: native SKALD table missing")
end
local Class = wh.entitymodule.ItemDescriptorTrigger
if type(Class) ~= "table" or Class.__cpp_name ~= "wh::entitymodule::C_ItemDescriptorTrigger" then
    error("[LuaUtils] SKALD class identity mismatch at wh.entitymodule.ItemDescriptorTrigger")
end
local generated = rawget(Class, "__skald_generated_node")
if generated ~= nil then
    if generated ~= "wh::entitymodule::C_ItemDescriptorTrigger" then
        error("[LuaUtils] incompatible generated SKALD node at wh.entitymodule.ItemDescriptorTrigger")
    end
    return Class
end
local __applyUpdates = Skald.__applyUpdates
if type(__applyUpdates) ~= "function" then
    error("[LuaUtils] SKALD update dispatcher missing")
end
local __registerNode = Skald.__registerNode
if type(__registerNode) ~= "function" then
    error("[LuaUtils] SKALD node registrar missing")
end
local __invalidateNode = Skald.__invalidateNode
if type(__invalidateNode) ~= "function" then
    error("[LuaUtils] SKALD node invalidator missing")
end
local function readonly(values, label)
    return setmetatable({}, {
        __index = values,
        __newindex = function()
            error(label .. " is read-only", 2)
        end,
        __metatable = false,
    })
end

---@class wh.entitymodule.ItemDescriptorTrigger.CreateArgs
---@field IsActive boolean? Native IsActive data input.
---@field Inventory userdata? Native Inventory object input (RTTR handle or wrapper).
---@field ItemDescriptor userdata? Native ItemDescriptor object input (RTTR handle or wrapper).

---@alias wh.entitymodule.ItemDescriptorTrigger.OutputName
---| "OnTargetAmountAcquire"
---| "OnAcquire"
---| "OnTargetAmountLose"
---| "OnLose"
---| "OnAfterLose"
---| "Tracker"
---| "Item"

---@class wh.entitymodule.ItemDescriptorTrigger.Outputs
---@field OnTargetAmountAcquire "OnTargetAmountAcquire" Native trigger output.
---@field OnAcquire "OnAcquire" Native trigger output.
---@field OnTargetAmountLose "OnTargetAmountLose" Native trigger output.
---@field OnLose "OnLose" Native trigger output.
---@field OnAfterLose "OnAfterLose" Native trigger output.
---@field Tracker "Tracker" Native data output.
---@field Item "Item" Native data output.

---@class wh.entitymodule.ItemDescriptorTrigger
---@field private __skald_handle integer?
---@field private __skald_outputs table?
---@field Outputs wh.entitymodule.ItemDescriptorTrigger.Outputs

if rawget(Class, "Outputs") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.ItemDescriptorTrigger.Outputs")
end
local __outputValues = { OnTargetAmountAcquire = "OnTargetAmountAcquire", OnAcquire = "OnAcquire", OnTargetAmountLose = "OnTargetAmountLose", OnLose = "OnLose", OnAfterLose = "OnAfterLose", Tracker = "Tracker", Item = "Item" }
Class.Outputs = readonly(__outputValues, "wh.entitymodule.ItemDescriptorTrigger.Outputs")
local __outputSpecs = {
    ["OnTargetAmountAcquire"] = { kind = "trigger", name = "OnTargetAmountAcquire", wrap = nil },
    ["OnAcquire"] = { kind = "trigger", name = "OnAcquire", wrap = nil },
    ["OnTargetAmountLose"] = { kind = "trigger", name = "OnTargetAmountLose", wrap = nil },
    ["OnLose"] = { kind = "trigger", name = "OnLose", wrap = nil },
    ["OnAfterLose"] = { kind = "trigger", name = "OnAfterLose", wrap = nil },
    ["Tracker"] = { kind = "data", name = "Tracker", wrap = wh.questmodule.nodes.Tracker.__from_handle },
    ["Item"] = { kind = "data", name = "Item", wrap = wh.entitymodule.Item.__from_handle },
}

local __createInputs = {
    ["IsActive"] = true,
    ["Inventory"] = true,
    ["ItemDescriptor"] = true,
}
local __requiredInputs = {
}
local __arrayInputs = {
}

if rawget(Class, "Create") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.ItemDescriptorTrigger.Create")
end
---@param args wh.entitymodule.ItemDescriptorTrigger.CreateArgs
---@return wh.entitymodule.ItemDescriptorTrigger? node
---@return string? error
function Class.Create(args)
    if type(args) ~= "table" then
        return nil, "wh.entitymodule.ItemDescriptorTrigger.Create expects an argument table"
    end
    for name in pairs(args) do
        if not __createInputs[name] then
            return nil, "unknown creation input '" .. tostring(name) ..
                "' for wh.entitymodule.ItemDescriptorTrigger"
        end
    end
    for name in pairs(__requiredInputs) do
        if rawget(args, name) == nil then
            return nil, "missing required creation input '" .. name ..
                "' for wh.entitymodule.ItemDescriptorTrigger"
        end
    end
    local nativeArgs = {}
    local metaArgs = {}
    for name, value in pairs(args) do
        if not metaArgs[name] then
            local arrayAlias = __arrayInputs[name]
            if arrayAlias and type(value) == "table" and
                    value.__rttr_handle == nil then
                local arrayValue = { __array_type = arrayAlias }
                for i, element in ipairs(value) do
                    if type(element) == "table" and
                        element.__rttr_handle ~= nil then
                        arrayValue[i] = element.__rttr_handle
                    else
                        arrayValue[i] = element
                    end
                end
                nativeArgs[name] = arrayValue
            elseif type(value) == "table" and value.__rttr_handle ~= nil then
                nativeArgs[name] = value.__rttr_handle
            else
                nativeArgs[name] = value
            end
        end
    end
    local skaldHandle, rttrHandleOrError =
        raw_skald.CreateNode("ItemDescriptorTrigger", nativeArgs)
    if skaldHandle == nil then return nil, rttrHandleOrError end
    local object = Class.__from_handle(rttrHandleOrError)
    object.__skald_handle = skaldHandle
    object.__skald_outputs = {}
    local registered, registerError = __registerNode(object)
    if not registered then
        raw_skald.DestroyNode(skaldHandle)
        return nil, registerError
    end
    local sentinel = newproxy(true)
    getmetatable(sentinel).__gc = function()
        if rawget(object, "__skald_handle") ~= nil then
            raw_skald.QueueDestroy(skaldHandle)
        end
    end
    object.__skald_gc = sentinel
    return object
end

---@overload fun(self: wh.entitymodule.ItemDescriptorTrigger, output: "OnTargetAmountAcquire", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.entitymodule.ItemDescriptorTrigger, output: "OnAcquire", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.entitymodule.ItemDescriptorTrigger, output: "OnTargetAmountLose", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.entitymodule.ItemDescriptorTrigger, output: "OnLose", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.entitymodule.ItemDescriptorTrigger, output: "OnAfterLose", callback: fun()): Skald.OutputConnection?, string?
---@overload fun(self: wh.entitymodule.ItemDescriptorTrigger, output: "Tracker"): Skald.OutputValue<wh.questmodule.nodes.Tracker>?, string?
---@overload fun(self: wh.entitymodule.ItemDescriptorTrigger, output: "Item"): Skald.OutputValue<wh.entitymodule.Item>?, string?
if rawget(Class, "BindOutput") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.ItemDescriptorTrigger.BindOutput")
end
function Class:BindOutput(output, callback)
    if self.__skald_handle == nil then
        return nil, "wh.entitymodule.ItemDescriptorTrigger is not a live LuaUtils-created node"
    end
    local spec = __outputSpecs[output]
    if spec == nil then
        return nil, "unknown output selector for wh.entitymodule.ItemDescriptorTrigger"
    end
    local outputs = rawget(self, "__skald_outputs")
    if type(outputs) ~= "table" then
        return nil, "SKALD output registry is unavailable"
    end
    if spec.kind == "trigger" then
        if type(callback) ~= "function" then
            return nil, "trigger output '" .. spec.name ..
                "' requires a callback"
        end
        local handle, err = raw_skald.BindTriggerOutput(
            self.__skald_handle, spec.name, callback)
        if handle == nil then return nil, err end
        local connection = Skald.OutputConnection.__new(handle)
        outputs[#outputs + 1] = connection
        return connection
    end
    if callback ~= nil then
        return nil, "data output '" .. spec.name ..
            "' does not accept a callback"
    end
    local handle, valueOrError = raw_skald.BindDataOutput(
        self.__skald_handle, spec.name)
    if handle == nil then return nil, valueOrError end
    local holder = Skald.OutputValue.__new(
        handle, valueOrError, spec.wrap)
    outputs[#outputs + 1] = holder
    return holder
end

if rawget(Class, "Activate") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.ItemDescriptorTrigger.Activate")
end
---@return boolean? ok
---@return string? error
function Class:Activate()
    if self.__skald_handle == nil then
        return nil, "wh.entitymodule.ItemDescriptorTrigger is not a live LuaUtils-created node"
    end
    local updates, err = raw_skald.SetActivation(
        self.__skald_handle, true)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Deactivate") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.ItemDescriptorTrigger.Deactivate")
end
---@return boolean? ok
---@return string? error
function Class:Deactivate()
    if self.__skald_handle == nil then
        return nil, "wh.entitymodule.ItemDescriptorTrigger is not a live LuaUtils-created node"
    end
    local updates, err = raw_skald.SetActivation(
        self.__skald_handle, false)
    if updates == nil then return nil, err end
    __applyUpdates(updates)
    return true
end

if rawget(Class, "Destroy") ~= nil then
    error("[LuaUtils] public symbol collision at wh.entitymodule.ItemDescriptorTrigger.Destroy")
end
---@return boolean? ok
---@return string? error
function Class:Destroy()
    if self.__skald_handle == nil then
        return nil, "wh.entitymodule.ItemDescriptorTrigger is not a live LuaUtils-created node"
    end
    local ok, err = raw_skald.DestroyNode(self.__skald_handle)
    if ok then __invalidateNode(self) end
    return ok, err
end

local __rttrRelease = Class.Release
function Class:Release()
    if self.__skald_handle ~= nil then return self:Destroy() end
    return __rttrRelease(self)
end
Class.__skald_generated_node = "wh::entitymodule::C_ItemDescriptorTrigger"

return Class
